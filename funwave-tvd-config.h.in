
#ifndef __FUNWAVE_TVD_CONFIG_H__
#define __FUNWAVE_TVD_CONFIG_H__

#define FUNWAVE_TVD_VERSION_STRING "@PROJECT_VERSION@"
#define FUNWAVE_TVD_VERSION_MAJOR @PROJECT_VERSION_MAJOR@
#define FUNWAVE_TVD_VERSION_MINOR @PROJECT_VERSION_MINOR@
#define FUNWAVE_TVD_VERSION_PATCH @PROJECT_VERSION_PATCH@

#include "funwave-tvd-githash.h"

#define FUNWAVE_TVD_COMPILER_STRING "@CMAKE_Fortran_COMPILER_ID@"

#define FUNWAVE_TVD_FEATURES_STRING "@FUNWAVE_TVD_FEATURES_STRING@"

#cmakedefine ENABLE_COUPLING
#cmakedefine ENABLE_ZALPHA
#cmakedefine ENABLE_MANNING
#cmakedefine ENABLE_VESSEL
#cmakedefine ENABLE_METEO
#cmakedefine ENABLE_WIND
#cmakedefine ENABLE_SEDIMENT
#cmakedefine ENABLE_CHECK_MASS_CONSERVATION
#cmakedefine ENABLE_TMP
#cmakedefine ENABLE_TRACKING
#cmakedefine ENABLE_FOAM
#cmakedefine ENABLE_PRECIPITATION
#cmakedefine ENABLE_SUBGRID
#cmakedefine ENABLE_DEEP_DRAFT_VESSEL
#cmakedefine ENABLE_FILTERING
#cmakedefine ENABLE_ITERATION
#cmakedefine ENABLE_AB_OUTPUT
#cmakedefine ENABLE_SPHERICAL_IJ_STATION
#cmakedefine ENABLE_UseEtaScreen

#cmakedefine ENABLE_VESSEL_PANEL_SOURCE
#cmakedefine ENABLE_REALISTIC_VESSEL_BODY
#cmakedefine ENABLE_PROPELLER

#define FUNWAVE_TVD_PRECISION "@FP_PRECISION@"
#cmakedefine ENABLE_DOUBLE_PRECISION

#define FUNWAVE_TVD_COORD_SYSTEM "@COORD_SYSTEM@"
#cmakedefine ENABLE_COORD_SPHERICAL

#cmakedefine ENABLE_MPI
#cmakedefine HAVE_INTEL_FORTRAN
#cmakedefine ENABLE_DEBUG
#cmakedefine ENABLE_SEDIMENT_DEBUG



#if defined(ENABLE_COUPLING) && ! defined(COUPLING)
#   define COUPLING
#endif
#if defined(ENABLE_ZALPHA) && ! defined(ZALPHA)
#   define ZALPHA
#endif
#if defined(ENABLE_MANNING) && ! defined(MANNING)
#   define MANNING
#endif
#if defined(ENABLE_VESSEL) && ! defined(VESSEL)
#   define VESSEL
#endif
#if defined(ENABLE_METEO) && ! defined(METEO)
#   define METEO
#endif
#if defined(ENABLE_WIND) && ! defined(WIND)
#   define WIND
#endif
#if defined(ENABLE_SEDIMENT) && ! defined(SEDIMENT)
#   define SEDIMENT
#endif
#if defined(ENABLE_CHECK_MASS_CONSERVATION) && ! defined(CHECK_MASS_CONSERVATION)
#   define CHECK_MASS_CONSERVATION
#endif
#if defined(ENABLE_TMP) && ! defined(TMP)
#   define TMP
#endif
#if defined(ENABLE_TRACKING) && ! defined(TRACKING)
#   define TRACKING
#endif
#if defined(ENABLE_FOAM) && ! defined(FOAM)
#   define FOAM
#endif
#if defined(ENABLE_PRECIPITATION) && ! defined(PRECIPITATION)
#   define PRECIPITATION
#endif
#if defined(ENABLE_SUBGRID) && ! defined(SUBGRID)
#   define SUBGRID
#endif
#if defined(ENABLE_DEEP_DRAFT_VESSEL) && ! defined(DEEP_DRAFT_VESSEL)
#   define DEEP_DRAFT_VESSEL
#endif
#if defined(ENABLE_FILTERING) && ! defined(FILTERING)
#   define FILTERING
#endif
#if defined(ENABLE_ITERATION) && ! defined(ITERATION)
#   define ITERATION
#endif
#if defined(ENABLE_AB_OUTPUT) && ! defined(AB_OUTPUT)
#   define AB_OUTPUT
#endif
#if defined(ENABLE_SPHERICAL_IJ_STATION) && ! defined(SPHERICAL_IJ_STATION)
#   define SPHERICAL_IJ_STATION
#endif
#if defined(ENABLE_UseEtaScreen) && ! defined(UseEtaScreen)
#   define UseEtaScreen
#endif

#if defined(ENABLE_VESSEL_PANEL_SOURCE) && ! defined(VESSEL_PANEL_SOURCE)
#   define VESSEL_PANEL_SOURCE
#endif
#if defined(ENABLE_REALISTIC_VESSEL_BODY) && ! defined(REALISTIC_VESSEL_BODY)
#   define REALISTIC_VESSEL_BODY
#endif
#if defined(ENABLE_PROPELLER) && ! defined(PROPELLER)
#   define PROPELLER
#endif

#if defined(ENABLE_DOUBLE_PRECISION) && ! defined(DOUBLE_PRECISION)
#   define DOUBLE_PRECISION
#endif
#ifndef ENABLE_COORD_SPHERICAL
#   define CARTESIAN
#endif
#if defined(ENABLE_MPI) && ! defined(PARALLEL)
#   define PARALLEL
#endif
#if defined(HAVE_INTEL_FORTRAN) && ! defined(INTEL)
#   define INTEL
#endif
#if defined(ENABLE_DEBUG) && ! defined(DEBUG)
#   define DEBUG
#endif
#if defined(ENABLE_SEDIMENT_DEBUG) && ! defined(SEDIMENT_DEBUG)
#   define SEDIMENT_DEBUG
#endif

#endif /* __FUNWAVE_TVD_CONFIG_H__ */

